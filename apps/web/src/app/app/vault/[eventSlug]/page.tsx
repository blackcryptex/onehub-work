// Re-export the Event Vault detail page component from (app)/vault/[eventSlug]
// This makes /app/vault/[eventSlug] a valid route while reusing the existing component
export { default } from "@/app/(app)/vault/[eventSlug]/page";

