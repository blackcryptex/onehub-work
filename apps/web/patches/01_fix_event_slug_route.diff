--- a/apps/web/src/app/api/events/[eventSlug]/route.ts
+++ b/apps/web/src/app/api/events/[eventSlug]/route.ts
@@ -38,7 +38,7 @@ export async function GET(
         proposals: {
-          include: { payouts: { select: { status: true, amountCents: true, createdAt: true } } },
+          // Removed payouts include - relation may not exist
           orderBy: { createdAt: "desc" },
         },
         activities: { orderBy: { at: "desc" }, take: 20 },
@@ -52,7 +52,8 @@ export async function GET(
     }
 
     // Check if user has access to this event's org
-    if (event.org.members.length === 0) {
+    const org = event.org;
+    if (!org || org.members.length === 0) {
       return NextResponse.json({ error: "Forbidden" }, { status: 403 });
     }

